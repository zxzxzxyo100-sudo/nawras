Options -MultiViews
RewriteEngine On
RewriteBase /staging/

# أعد توجيه كل الطلبات لـ index.html (Single Page Application)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.html [L]
